# C

## Basic
+ hello -> hello
+ size  -> 数据类型
+ var   -> 变量
+ const -> 常量
+ auto  -> 存储类
+ count -> 运算符
+ judge -> 判断
+ loop -> 循环
+ func -> 函数
+ action -> 作用域
+ array -> 数组
+ enum -> 枚举
+ hand -> 指针
+ fhand -> 函数指针
+ char -> 字符串
+ struct -> 结构体
+ union -> 位域
+ typedef -> typedef
+ inout -> 输入&输出
+ file -> 文件读写
+ cpp -> 预处理器
+ headf -> 头文件
+ cast  -> 强制类型转换
+ error -> 错误处理
+ recur -> 递归
+ vars -> 可变参数
+ m-manager -> 内存管理
+ args  -> 命令行参数
+ sort -> 排序算法
## Deep