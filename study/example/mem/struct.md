## 结构体

### 结构体数组  
```C
struct 结构体类型
{
    成员;
} 数组名[长度];

/****或者这样****/

struct 结构体类型
{
    成员;
};
struct 结构体类型 数组名[长度];
```
### 结构体指针

```C
struct Info* pinfo;

pinfo = &info

```

*  (*结构体指针).成员名
*  结构体指针->成员名




